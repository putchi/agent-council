# Requirements

- `codex` CLI must be installed and authenticated
- `node` (v16+) must be installed — used for JSON serialization and job orchestration
- No API keys needed in the skill. All members and the chairman use the local Codex CLI configuration.

## Verify setup

```bash
codex --version               # should show Codex CLI version
node --version                # v16+
```
