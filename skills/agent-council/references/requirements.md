# Requirements

- `claude` CLI must be installed and authenticated (Claude Code subscription required)
- `node` (v16+) must be installed — used for JSON serialization and job orchestration
- No API keys needed. All members and the chairman use Claude Code CLI.

## Verify setup

```bash
claude --version              # should show claude CLI version
node --version                # v16+
```
